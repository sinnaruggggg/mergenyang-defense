# Reference-locked workshop separation

- Canvas: `941 x 1672` pixels, portrait 9:16.
- Approved source: `reference-full.png`.
- Exact placement data: `placement.json`.
- Reassembled reference: `assembled/reference-locked-assembly.png`.
- Visible prop blocks: `layers/reference-crops/`.

All prop blocks are copied directly from the approved source at `scale: 1`. They have not been redrawn, resized, perspective-corrected, or recolored. Place each PNG at its `x`, `y`, and `z` in `placement.json` to retain the approved camera and lighting.

`layers/background-clean.png` is a reconstruction only for content that was hidden by removable objects. It is not used to validate the approved final frame. The reference-locked assembly is the visual ground truth.

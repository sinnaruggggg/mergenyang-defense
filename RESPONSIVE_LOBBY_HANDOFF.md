# 긴 화면 대응 작업 인계서

## 목적

기존 대장간 로비의 기준 해상도는 `1080 × 1920`(9:16)이다. 세로가 더 긴 기기에서 캔버스를 9:16으로 축소해 중앙 배치하면, 스크린샷처럼 위아래에 갈색 여백이 생긴다.

이번 작업은 **대장간 로비에 한해서** 게임 캔버스의 논리 높이를 기기 높이만큼 확장하여 여백을 없앴다. 버튼·캐릭터·UI 이미지 자체의 가로세로 비율은 바꾸지 않았다.

## 적용 원칙

| 영역 | 처리 방식 |
| --- | --- |
| 배경 | 확대 비율을 `max(화면 폭 / 배경 폭, 확장 높이 / 배경 높이)`로 계산해 세로 끝까지 채운다. |
| 상단 HUD | 화면 상단에 고정한다. 노치 안전영역이 있으면 그만큼 아래로 이동한다. |
| 하단 UI | 추가된 세로 공간(`extra`)만큼 아래로 이동한다. 방치 보상, 공방 레벨, 성장 버튼, 모험 버튼, 탭 바가 한 묶음으로 이동한다. |
| 대장장이 | 방치 보상 박스 뒤에 가려지는 구도를 유지하기 위해 하단 UI와 같은 거리만큼 아래로 이동한다. |
| 다른 메뉴 | 기존 `1920` 높이를 그대로 사용한다. 고양이·전투·상점·랭킹의 레이아웃은 변경하지 않는다. |

예를 들어 CSS 픽셀 기준 `390 × 844` 화면에서는 로비 논리 캔버스 높이가 `2337`이 되고, `extra`는 `417`이다. 상단은 기존 좌표를 유지하고 하단 묶음만 `417px` 아래로 내려간다.

## 수정 파일

### `game/js/core.js`

- `View` 상태를 추가했다.
  - `lobby`: 현재 로비인지 여부
  - `height`: 실제 사용할 논리 캔버스 높이
  - `extra`: `height - 1920`
  - `top`, `bottom`: CSS safe-area 값을 논리 좌표로 환산한 값
- `fitCanvas()`가 로비에서만 캔버스 높이를 확장하도록 했다.
- 입력 좌표의 Y축 계산을 `canvas.height` 기준으로 변경했다. 확장된 하단 버튼도 누를 수 있도록 하는 필수 변경이다.
- `UI.offsetY`를 추가했다. 그려진 위치와 동일하게 클릭 판정도 이동시키기 위해 `UI.hit()`과 `UI.pressed()`에서 적용한다.

### `game/js/main.js`

- `Game.enter()` 시 `View.lobby = name === 'lobby'`를 설정한 뒤 `fitCanvas()`를 재호출한다.
- 로비 팝업은 확장된 화면 중앙에 나타나도록 `View.extra / 2`만큼 이동시킨다.
- 전환 페이드가 확장된 캔버스 전체를 덮도록 `canvas.height`를 사용한다.

### `game/js/scenes.js`

- `LobbyScene.draw()` 안에서 `lowerY = View.extra - View.bottom`을 계산한다.
- 배경은 `View.height`를 기준으로 cover 방식으로 그린다.
- 상단 HUD·제목·도감·도움말은 `View.top`만 적용한다.
- 대장장이와 하단 UI 묶음은 `lowerY`만큼 이동한다.
- 방치 보상 수령 시 골드가 날아가는 시작점도 이동한 보상 박스의 좌표를 사용한다.
- 팝업의 어두운 오버레이도 확장된 높이 전체를 덮는다.

### `game/index.html`

- `env(safe-area-inset-top)`, `env(safe-area-inset-bottom)`을 CSS 변수로 노출했다.

## 유지해야 할 제약

1. `W = 1080`, 기준 UI 좌표와 원본 PNG 에셋의 비율은 유지한다.
2. 다른 메뉴는 사용자가 승인한 상태이므로 로비의 긴 화면 대응을 다른 씬에 전파하지 않는다.
3. 이미지 UI는 `imgFit()`으로 그린다. 폭만 늘려서 버튼을 찌그러뜨리지 않는다.
4. 로비에서 새 하단 UI를 추가할 경우, 기존 하단 묶음과 같이 `lowerY` 컨텍스트 안에 배치해야 한다.
5. 상단에 새 UI를 추가할 경우 `View.top` 컨텍스트 안에 배치해야 한다.

## 검수 결과

- `390 × 844` 긴 화면에서 위아래 갈색 여백 없이 대장간 배경이 화면을 채우는 것을 확인했다.
- `540 × 960` 표준 9:16에서는 이전과 같은 논리 높이 `1920`으로 렌더되는 것을 확인했다.
- 긴 화면에서 상단 설정 버튼과 하단 공방 성장 버튼의 클릭 전환을 확인했다.

검수 스크린샷: `game/dev/lobby-tall-screen.png`, `game/dev/lobby-standard-screen.png`
